// Phase 10: Recursive expand/collapse tree renderer.
// Props: { nodeId: string | null, depth: number }
// On expand -> subscribeToChildren(nodeId); on collapse -> unsubscribes listener.
export default function RoadmapTree({ nodeId = null, depth = 0 }) {
  return null; // Phase 10 implementation
}
